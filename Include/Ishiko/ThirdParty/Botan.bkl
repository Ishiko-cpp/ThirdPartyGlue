setting BOTAN_ROOT
{
    help = "Path to the Botan installation";
    default = '$(BOTAN)';
}

template Botan
{
    includedirs += $(BOTAN_ROOT)/include/botan-2;
    libdirs += $(BOTAN_ROOT)/lib;
    libs += botan;
}
