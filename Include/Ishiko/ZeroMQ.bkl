setting ZEROMQ_ROOT
{
    help = "Path to the ZeroMQ installation";
    default = '$(ZEROMQ)';
}

template ZeroMQ
{
    includedirs += $(ZEROMQ_ROOT)/libzmq/include;
    includedirs += $(ZEROMQ_ROOT)/cppzmq;

    libdirs += $(ZEROMQ_ROOT)/libzmq/bin/x64/Debug/v140/static;
    libs += libzmq;
}
